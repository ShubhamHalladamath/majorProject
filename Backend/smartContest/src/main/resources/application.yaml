spring:
  application:
    name: smartContest

  datasource:
    url: jdbc:mysql://localhost:3306/smart_contest
    username: root
    password: NewPassword123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true